import { FontTituloDirective } from './font-titulo.directive';

describe('FontTituloDirective', () => {
  it('should create an instance', () => {
    const directive = new FontTituloDirective();
    expect(directive).toBeTruthy();
  });
});
