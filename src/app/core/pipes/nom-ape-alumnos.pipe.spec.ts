import { NomApeAlumnosPipe } from './nom-ape-alumnos.pipe';

describe('NomApeAlumnosPipe', () => {
  it('create an instance', () => {
    const pipe = new NomApeAlumnosPipe();
    expect(pipe).toBeTruthy();
  });
});
